{
  "title": "Instagram Auto Follow/Unfollow Input",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "sessionCookie": {
      "title": "Instagram Session Cookie (sessionid)",
      "type": "string",
      "description": "Your Instagram session cookie (sessionid). Get it from browser DevTools > Application > Cookies > instagram.com > sessionid",
      "editor": "textarea",
      "isSecret": true
    },
    "accountType": {
      "title": "Account Type",
      "type": "string",
      "description": "Select your account age/activity level to auto-set safe daily follow limits",
      "enum": ["brand_new", "new", "mature", "aged"],
      "enumTitles": [
        "Brand New Account (5-10/day - First 2 weeks)",
        "New Account (10-15/day - First 3 months)",
        "Mature Account (15-25/day - 3+ months old)",
        "Aged Account (25-50/day - 1+ years old)"
      ],
      "default": "mature"
    },
    "action": {
      "title": "Action to Perform",
      "type": "string",
      "description": "Choose whether to follow or unfollow users",
      "enum": ["follow", "unfollow"],
      "enumTitles": ["Follow Users", "Unfollow Users"],
      "default": "follow"
    },
    "maxFollowsPerRun": {
      "title": "Max Actions Per Run",
      "type": "integer",
      "description": "Never exceed 50 actions in one run. Recommended: 5-10 (brand new), 10-15 (new), 15-25 (mature), 25-50 (aged)",
      "default": 15,
      "minimum": 1,
      "maximum": 50
    },
    "delayBetweenFollows": {
      "title": "Delay Between Actions (seconds)",
      "type": "integer",
      "description": "Time to wait between actions to avoid detection. Recommended 30-60s, but can be 0 for fast runs",
      "default": 45,
      "minimum": 0,
      "maximum": 300
    },
    "randomDelayVariation": {
      "title": "Random Delay Variation (±seconds)",
      "type": "integer",
      "description": "Random variation to make activity look human. Recommended: 15-30 seconds",
      "default": 20,
      "minimum": 5,
      "maximum": 60
    },
    "usersToFollow": {
      "title": "Users to Follow/Unfollow",
      "type": "array",
      "description": "List of Instagram usernames to follow or unfollow (one per line)",
      "editor": "stringList"
    }
  },
  "required": ["sessionCookie", "usersToFollow"]
}
