{
  "actorOutputSchemaVersion": 1,
  "title": "Instagram Auto Follow Results",
  "description": "Successful follows/requests are in the dataset (billable); the full per-user report and summary are in the RUN_REPORT key-value store record",
  "properties": {
    "results": {
      "type": "string",
      "title": "Successful Follows (billable)",
      "template": "{{links.apiDefaultDatasetUrl}}/items"
    },
    "runReport": {
      "type": "string",
      "title": "Full Run Report (all statuses + summary)",
      "template": "{{links.apiDefaultKeyValueStoreUrl}}/records/RUN_REPORT"
    }
  }
}
